export const actions = {
    multiply: (value, operationValue) => {
        return value * operationValue;
    },
    split: (value, operationValue) => {
        return value / operationValue;
    },
    same: (value, operationValue) => {
        return value * operationValue;
    } 
}

